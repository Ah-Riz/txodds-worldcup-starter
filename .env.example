# Network: devnet (free airdrop, level 1 only) or mainnet (free levels 1 + 12)
NETWORK=devnet

# Free World Cup tier: 1 = 60s delay, 12 = real-time (mainnet only)
SERVICE_LEVEL=1

# Subscription length (weeks)
DURATION_WEEKS=4

# Filled in by `node setup.mjs`:
# JWT=...
# API_TOKEN=...
